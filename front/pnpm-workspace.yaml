# Generated by `vp install`. Enable the install scripts pnpm would
# otherwise block. esbuild ships a native binary that needs its
# postinstall to run for Vite's dev/build pipeline.
allowBuilds:
  esbuild: true

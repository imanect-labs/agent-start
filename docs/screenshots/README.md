# Screenshots / demo media

Place screenshots and demo GIFs here. Reference them from `README.md` once added.

Suggested captures:

- `launcher.png` — project list + start sheet.
- `terminal.png` — running PTY session with scrollback.
- `worktree.png` — worktree-mode session with diff viewer.
- `mobile.png` — same UI on a phone via tailnet.

Keep individual files under ~500 KB and prefer PNG / animated WebP / MP4 over
GIF for size.
